# Feed Frontend

Vanilla HTML, CSS, and JavaScript frontend for Feed.

The frontend calls `http://localhost:8080` by default.

To point it at another backend from the browser console:

```js
localStorage.setItem("feed_api_base", "https://your-api-domain.com");
```
